# Styling Components – classnames & cn

Combine classes with `clsx` or `tailwind-merge`. Create a `cn` helper to merge and deduplicate.

> 📘 Next: [Tailwind with CSS Modules](04-tailwind-with-css-modules.md)
