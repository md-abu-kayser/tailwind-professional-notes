# Responsive Design Breakpoints

Prefix utilities with breakpoint: `sm:`, `md:`, `lg:`, `xl:`, `2xl:`. Mobile‑first: unprefixed classes apply from zero width up.

```html
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3"></div>
```
