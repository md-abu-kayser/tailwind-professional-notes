# Group Hover & Peer

`group` and `peer` utilities for styling based on parent or sibling state.

```html
<div class="group">
  <div class="group-hover:bg-blue-500">...</div>
</div>
<input class="peer" type="checkbox" />
<span class="peer-checked:text-blue-500">Checked</span>
```
