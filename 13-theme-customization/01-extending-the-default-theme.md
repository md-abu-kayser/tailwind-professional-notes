# Extending the Default Theme

Use `theme.extend` to add to existing scale without overriding.

```js
theme: {
  extend: {
    colors: { brand: { 500: '#ff0000' } },
    spacing: { '128': '32rem' },
  }
}
```
